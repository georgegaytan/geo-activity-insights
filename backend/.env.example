# Local backend env (when running uvicorn directly, not via Docker)
# Copy to backend/.env and fill in values as needed.

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/geo_activities
REDIS_URL=redis://localhost:6379/0
STRAVA_CLIENT_ID=199669
STRAVA_CLIENT_SECRET=your_real_strava_client_secret_here
